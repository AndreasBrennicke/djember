<div class="book book-detail">
  <div class="book-title">
    {{model.title}}
  </div>
  <div class="book-author">
    {{model.author}}
  </div>
  <div class="book-description">
    {{model.description}}
  </div>
</div>

<div class="actions {{if confirmingDelete 'space-out'}}">
  {{#if confirmingDelete}}
    <div class="danger">
      Are you sure you want to delete this book?
    </div>
      <button {{action 'delete' model}}>Delete</button>
      <button {{action (mut confirmingDelete)false}}>Cancel</button>
  {{else}}
      <button {{action (mut confirmingDelete) true}}>Delete</button>
  {{/if}}
</div>